import React from 'react';
import { RegisterForm } from '../components';

const RegisterPage = () => {
  return (
    <div>
      {/* Empty container for team to design register page layout */}
      {/* RegisterForm component available with all logic intact */}
      <RegisterForm />
    </div>
  );
};

export default RegisterPage;
